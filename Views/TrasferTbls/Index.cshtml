@model IEnumerable<BloodBank.Models.TrasferTbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BloodTbl.BGroup)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PatientTbl.PName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BloodTbl.BGroup)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PatientTbl.PName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TNum }) |
            @Html.ActionLink("Details", "Details", new { id=item.TNum }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TNum })
        </td>
    </tr>
}

</table>
