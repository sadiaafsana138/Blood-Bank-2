@model IEnumerable<BloodBank.Models.BloodTbl>

@{
    ViewBag.Title = "Index";
}

<h2 style="color:red;">AVAILABLE BLOODS </h2>

<p>
    @Html.ActionLink("ADD NEW STOCK", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BGroup)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BStock)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.BGroup)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.BStock)
    </td>
    <td>
        @Html.ActionLink("Update", "Edit", new { id = item.BGroup }) |
        @Html.ActionLink("Details", "Details", new { id = item.BGroup }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.BGroup })
    </td>
</tr>
}

</table>
