@model IEnumerable<BloodBank.Models.CollectTbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BloodTbl.BGroup)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DonorTbl.DName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BloodTbl.BGroup)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonorTbl.DName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CNum }) |
            @Html.ActionLink("Details", "Details", new { id=item.CNum }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CNum })
        </td>
    </tr>
}

</table>
